{
	"name": "brgykodego",
	"version": "0.4.0",
	"description": "",
	"homepage": "http://brgykodego.herokuapp.com/",
	"main": "src/app.js",
	"scripts": {
		"start": "node src/clearscreen.js && nodemon",
		"pages": "node src/pages.js",
		"clean": "rm -rf node_modules package-lock.json",
		"setup-database": "cat src/sql/create-database.sql src/sql/create-tables.sql src/sql/populate-tables.sql | mysql -u root",
		"setup": "npm run setup-database && npm install && npm run pages && npm test",
		"remove-database": "mysql -u root < src/sql/remove-database.sql",
		"remove": "test -d node_modules && npm run remove-database && npm run clean || echo Nothing to do.",
		"test": "node src/nodetest.js"
	},
	"author": "github.com/fbgabriana",
	"license": "ISC",
	"dependencies": {
		"file-api": "^0.10.4",
		"mime-types": "^2.1.35",
		"mysql": "^2.18.1",
		"nodemon": "^2.0.20",
		"parse-database-url": "^0.3.0"
	}
}
